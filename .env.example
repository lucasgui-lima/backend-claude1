# ============================================================
# Variáveis de ambiente do backend
# Copie este arquivo para .env e preencha os valores.
# Em produção (Render/Fly/Docker), defina diretamente no painel.
# ============================================================

# --- Banco de dados (Supabase / PostgreSQL) ---
# OPÇÃO A: URL completa (CARACTERES ESPECIAIS DA SENHA DEVEM SER URL-ENCODED)
#          ex.: se a senha é "S@gui126307", use "S%40gui126307"
#          OBS: Supabase exige SSL — adicione ?sslmode=require ao final
DATABASE_URL=postgresql+psycopg2://postgres:S%40gui126307@db.tltuihtisnmmabojukal.supabase.co:5432/postgres?sslmode=require

# OPÇÃO B: variáveis separadas (a senha é percent-encoded automaticamente)
# DB_USER=postgres
# DB_PASSWORD=S@gui126307
# DB_HOST=db.tltuihtisnmmabojukal.supabase.co
# DB_PORT=5432
# DB_NAME=postgres

# --- Autenticação ---
# Gere com: openssl rand -hex 32
SECRET_KEY=troque-por-uma-chave-aleatoria-longa-em-producao
ACCESS_TOKEN_EXPIRE_MINUTES=720

# --- CORS ---
# Em produção, restrinja às URLs do seu app.
# Use "*" apenas em desenvolvimento; com "*" o backend desativa credentials.
CORS_ORIGINS=*

# --- Ambiente ---
# Defina como "prod" para forçar checagens de produção (ex.: SECRET_KEY obrigatória).
ENV=prod
